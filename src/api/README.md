# API backend (`src/api`)

Canonical docs: **[docs/reference/legacy-api.md](../../docs/reference/legacy-api.md)**.

Quick start: `pip install -r requirements.txt` then `python main.py` from this folder.
